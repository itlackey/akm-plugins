---
name: bump-version
description: Bump semver across package.json files and tag the release.
keywords: [release, version, bump, semver, tag, publish]
---

Bump the version to {target} across all package manifests and create the matching git tag.
