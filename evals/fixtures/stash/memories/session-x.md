---
name: session-x
---

Scratch memory captured by an earlier agent turn. Present so that
`memories/session-x` resolves during ref validation — the auto-feedback
skip-list fixture (fb-008) is only meaningful if the ref is extractable.
